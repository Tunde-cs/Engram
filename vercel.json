{
  "framework": null,
  "installCommand": "pip install starlette",
  "buildCommand": "",
  "outputDirectory": ".",
  "functions": {
    "api/index.py": {
      "excludeFiles": "{src/**,tests/**,papers/**,assets/**,.pytest_cache/**}"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
